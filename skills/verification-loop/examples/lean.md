# Lean Commands

Use for daily checks with minimal cognitive load.

```bash
agentops loop doctor --repo-root . --md
agentops loop lean --repo-root .            # low-noise profile
agentops loop quality --repo-root .         # strict skill package quality pass
agentops loop autofix --repo-root . --threshold 9.0 --max-iterations 3
```
